// Generated by the Freon Language Generator.
import { observablepart, FreExpressionNode, FreParseLocation } from "@freon4dsl/core";
import { EntityExpression } from "./internal.js";

/**
 * Class IfExpression is the implementation of the concept with the same name in the language definition file.
 * It uses mobx decorators to enable parts of the language environment, e.g. the editor, to react
 * to the changes in the state of its properties.
 */
export class IfExpression extends EntityExpression implements FreExpressionNode {
    /**
     * A convenience method that creates an instance of this class
     * based on the properties defined in 'data'.
     * @param data partial object
     */
    static create(data: Partial<IfExpression>): IfExpression {
        const result = new IfExpression(data.$id);
        if (!!data.condition) {
            result.condition = data.condition;
        }
        if (!!data.whenTrue) {
            result.whenTrue = data.whenTrue;
        }
        if (!!data.whenFalse) {
            result.whenFalse = data.whenFalse;
        }
        if (!!data.appliedfeature) {
            result.appliedfeature = data.appliedfeature;
        }
        if (!!data.parseLocation) {
            result.parseLocation = data.parseLocation;
        }
        return result;
    }

    readonly $typename: string = "IfExpression"; // holds the metatype in the form of a string

    parseLocation: FreParseLocation; // if relevant, the location of this element within the source from which it is parsed

    condition: EntityExpression; // implementation of part 'condition'
    whenTrue: EntityExpression; // implementation of part 'whenTrue'
    whenFalse: EntityExpression; // implementation of part 'whenFalse'

    constructor(id?: string) {
        super(id);

        // Both 'observablepart' and 'observablepartlist' change the get and set of the attribute
        // such that the parent-part relationship is consistently maintained,
        // and make sure the part is observable. In lists no 'null' or 'undefined' values are allowed.
        observablepart(this, "condition");

        observablepart(this, "whenTrue");

        observablepart(this, "whenFalse");
    }

    /**
     * Returns the metatype of this instance in the form of a string.
     */
    freLanguageConcept(): string {
        return this.$typename;
    }

    /**
     * Returns true if this instance is a model concept.
     */
    freIsModel(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is a model unit.
     */
    freIsUnit(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is an expression concept.
     */
    freIsExpression(): boolean {
        return true;
    }

    /**
     * Returns true if this instance is a binary expression concept.
     */
    freIsBinaryExpression(): boolean {
        return false;
    }
    /**
     * A convenience method that copies this instance into a new object.
     */
    copy(): IfExpression {
        const result = new IfExpression();
        if (!!this.condition) {
            result.condition = this.condition.copy();
        }
        if (!!this.whenTrue) {
            result.whenTrue = this.whenTrue.copy();
        }
        if (!!this.whenFalse) {
            result.whenFalse = this.whenFalse.copy();
        }
        if (!!this.appliedfeature) {
            result.appliedfeature = this.appliedfeature.copy();
        }
        return result;
    }
    /**
     * Matches a partial instance of this class to this object
     * based on the properties defined in the partial.
     * @param toBeMatched
     */
    public match(toBeMatched: Partial<IfExpression>): boolean {
        let result: boolean = super.match(toBeMatched);
        if (result && !!toBeMatched.condition) {
            result = result && this.condition.match(toBeMatched.condition);
        }
        if (result && !!toBeMatched.whenTrue) {
            result = result && this.whenTrue.match(toBeMatched.whenTrue);
        }
        if (result && !!toBeMatched.whenFalse) {
            result = result && this.whenFalse.match(toBeMatched.whenFalse);
        }
        return result;
    }
}
