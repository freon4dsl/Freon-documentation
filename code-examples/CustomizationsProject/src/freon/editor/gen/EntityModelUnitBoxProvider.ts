// Generated by the Freon Language Generator.
import { Box, BoxUtil, FreBoxProvider, FreProjectionHandler, BoxFactory } from "@freon4dsl/core";

import { EntityModelUnit } from "../../language/gen/index.js";

/**
 * This class implements the box provider for a single node of type EntityModelUnit.
 * The box provider is able to create the (tree of) boxes for the node, based
 * on the projections that are currently selected by the user.
 * The top of the tree of boxes is always a box of type ElementBox, which is
 * a box that will never be rendered itself, only its content will. Thus, we
 * have a stable entry in the complete box tree for every FreNode node.
 */
export class EntityModelUnitBoxProvider extends FreBoxProvider {
    constructor(mainHandler: FreProjectionHandler) {
        super(mainHandler);
        this.knownBoxProjections = ["StartEditor", "default"];
        this.knownTableProjections = ["default"];
        this.conceptName = "EntityModelUnit";
    }

    protected getContent(projectionName: string): Box {
        // console.log("GET CONTENT " + this._node?.freId() + ' ' +  this._node?.freLanguageConcept() + ' ' + projectionName);
        // see if we need to use a custom projection
        if (!this.knownBoxProjections.includes(projectionName) && !this.knownTableProjections.includes(projectionName)) {
            const BOX: Box = this.mainHandler.executeCustomProjection(this._node, projectionName);
            if (!!BOX) {
                // found one, so return it
                return BOX;
            }
        } else {
            // select the box to return based on the projectionName
            if (projectionName === "StartEditor") {
                return this.getStartEditor();
            } else if (projectionName === "default") {
                return this.getDefault();
            }
        }
        // in all other cases, return the default
        return this.getDefault();
    }

    private getStartEditor(): Box {
        return BoxFactory.verticalLayout(this._node as EntityModelUnit, "EntityModelUnit-overall", "", [
            BoxFactory.horizontalLayout(
                this._node as EntityModelUnit,
                "EntityModelUnit-hlist-line-0",
                "",
                [
                    BoxUtil.labelBox(this._node as EntityModelUnit, "model", "top-1-line-0-item-0"),
                    BoxUtil.textBox(this._node as EntityModelUnit, "name"),
                    BoxUtil.labelBox(this._node as EntityModelUnit, "\{", "top-1-line-0-item-2"),
                ],
                { selectable: false },
            ),
            BoxUtil.labelBox(this._node as EntityModelUnit, "entities:", "top-1-line-1-item-0"),
            BoxUtil.indentBox(
                this._node as EntityModelUnit,
                4,
                "2",
                BoxUtil.verticalPartListBox(
                    this._node as EntityModelUnit,
                    (this._node as EntityModelUnit).entities,
                    "entities",
                    { text: "&&", type: "Terminator" },
                    this.mainHandler,
                ),
            ),
            BoxUtil.labelBox(this._node as EntityModelUnit, "model wide functions:", "top-1-line-3-item-0"),
            BoxUtil.indentBox(
                this._node as EntityModelUnit,
                4,
                "4",
                BoxUtil.horizontalPartListBox(
                    this._node as EntityModelUnit,
                    (this._node as EntityModelUnit).functions,
                    "functions",
                    { text: ";", type: "Separator" },
                    this.mainHandler,
                ),
            ),
            BoxUtil.labelBox(this._node as EntityModelUnit, "}", "top-1-line-5-item-0"),
        ]);
    }

    private getDefault(): Box {
        return BoxFactory.verticalLayout(this._node as EntityModelUnit, "EntityModelUnit-overall", "", [
            BoxFactory.horizontalLayout(
                this._node as EntityModelUnit,
                "EntityModelUnit-hlist-line-0",
                "",
                [
                    BoxUtil.labelBox(this._node as EntityModelUnit, "EntityModelUnit", "top-1-line-0-item-0"),
                    BoxUtil.textBox(this._node as EntityModelUnit, "name"),
                    BoxUtil.labelBox(this._node as EntityModelUnit, "\{", "top-1-line-0-item-2"),
                ],
                { selectable: false },
            ),
            BoxUtil.indentBox(
                this._node as EntityModelUnit,
                4,
                "1",
                BoxUtil.labelBox(this._node as EntityModelUnit, "functions", "top-1-line-1-item-0"),
            ),
            BoxUtil.indentBox(
                this._node as EntityModelUnit,
                8,
                "2",
                BoxUtil.verticalPartListBox(
                    this._node as EntityModelUnit,
                    (this._node as EntityModelUnit).functions,
                    "functions",
                    null,
                    this.mainHandler,
                ),
            ),
            BoxUtil.indentBox(
                this._node as EntityModelUnit,
                4,
                "3",
                BoxUtil.labelBox(this._node as EntityModelUnit, "entities", "top-1-line-3-item-0"),
            ),
            BoxUtil.indentBox(
                this._node as EntityModelUnit,
                8,
                "4",
                BoxUtil.verticalPartListBox(
                    this._node as EntityModelUnit,
                    (this._node as EntityModelUnit).entities,
                    "entities",
                    null,
                    this.mainHandler,
                ),
            ),
            BoxUtil.labelBox(this._node as EntityModelUnit, "}", "top-1-line-5-item-0"),
        ]);
    }
}
