// Generated by the Freon Language Generator.
import {
    FreModelUnit,
    MobxModelElementImpl,
    observableprim,
    observablepartlist,
    FreModel,
    FreLanguage,
    FreParseLocation,
    AST,
    FreUtils,
    matchElementList,
} from "@freon4dsl/core";
import { Topic, Flow, Test } from "./internal.js";

/**
 * Class Education is the implementation of the model with the same name in the language definition file.
 * It uses mobx decorators to enable parts of the language environment, e.g. the editor, to react
 * to any changes in the state of its properties.
 */
export class Education extends MobxModelElementImpl implements FreModel {
    /**
     * A convenience method that creates an instance of this class
     * based on the properties defined in 'data'.
     * @param data partial object
     */
    static create(data: Partial<Education>): Education {
        const result = new Education(data.$id);
        if (!!data.name) {
            result.name = data.name;
        }
        if (!!data.description) {
            result.description = data.description;
        }
        if (!!data.topic) {
            data.topic.forEach((x) => result.topic.push(x));
        }
        if (!!data.flow) {
            data.flow.forEach((x) => result.flow.push(x));
        }
        if (!!data.tests) {
            data.tests.forEach((x) => result.tests.push(x));
        }
        if (!!data.parseLocation) {
            result.parseLocation = data.parseLocation;
        }
        return result;
    }

    readonly $typename: string = "Education"; // holds the metatype in the form of a string
    $id: string = ""; // a unique identifier
    parseLocation: FreParseLocation; // if relevant, the location of this element within the source from which it is parsed
    name: string; // implementation of name
    description: string; // implementation of description
    topic: Topic[]; // implementation of part 'topic'
    flow: Flow[]; // implementation of part 'flow'
    tests: Test[]; // implementation of part 'tests'

    constructor(id?: string) {
        super();
        if (!!id) {
            this.$id = id;
        } else {
            this.$id = FreUtils.ID(); // uuid.v4();
        }

        // Both 'observablepart' and 'observablepartlist' change the get and set of the attribute
        // such that the parent-part relationship is consistently maintained,
        // and make sure the part is observable. In lists no 'null' or 'undefined' values are allowed.
        observablepartlist(this, "topic");
        observablepartlist(this, "flow");
        observablepartlist(this, "tests");
    }

    /**
     * Returns the metatype of this instance in the form of a string.
     */
    freLanguageConcept(): string {
        return this.$typename;
    }

    /**
     * Returns the unique identifier of this instance.
     */
    freId(): string {
        return this.$id;
    }

    /**
     * Returns true if this instance is a model concept.
     */
    freIsModel(): boolean {
        return true;
    }

    /**
     * Returns true if this instance is a model unit.
     */
    freIsUnit(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is an expression concept.
     */
    freIsExpression(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is a binary expression concept.
     */
    freIsBinaryExpression(): boolean {
        return false;
    }
    /**
     * A convenience method that copies this instance into a new object.
     */
    copy(): Education {
        const result = new Education();
        if (!!this.name) {
            result.name = this.name;
        }
        if (!!this.description) {
            result.description = this.description;
        }
        if (!!this.topic) {
            this.topic.forEach((x) => result.topic.push(x.copy()));
        }
        if (!!this.flow) {
            this.flow.forEach((x) => result.flow.push(x.copy()));
        }
        if (!!this.tests) {
            this.tests.forEach((x) => result.tests.push(x.copy()));
        }
        return result;
    }
    /**
     * Matches a partial instance of this class to this object
     * based on the properties defined in the partial.
     * @param toBeMatched
     */
    public match(toBeMatched: Partial<Education>): boolean {
        let result: boolean = true;
        if (result && toBeMatched.name !== null && toBeMatched.name !== undefined && toBeMatched.name.length > 0) {
            result = result && this.name === toBeMatched.name;
        }
        if (result && toBeMatched.description !== null && toBeMatched.description !== undefined && toBeMatched.description.length > 0) {
            result = result && this.description === toBeMatched.description;
        }
        if (result && !!toBeMatched.topic) {
            result = result && matchElementList(this.topic, toBeMatched.topic);
        }
        if (result && !!toBeMatched.flow) {
            result = result && matchElementList(this.flow, toBeMatched.flow);
        }
        if (result && !!toBeMatched.tests) {
            result = result && matchElementList(this.tests, toBeMatched.tests);
        }
        return result;
    }

    /**
     * A convenience method that finds a unit of this model based on its name and 'metatype'.
     * @param name
     * @param metatype
     */
    findUnit(name: string, metatype?: string): FreModelUnit {
        let result: FreModelUnit = null;
        const checkType = metatype !== undefined;
        result = this.getUnits().find(
            (mod) => mod.name === name && (checkType ? FreLanguage.getInstance().metaConformsToType(mod, metatype) : true),
        );
        if (!!result) {
            return result;
        }
        return null;
    }

    /**
     * Replaces a model unit by a new one. Used for swapping between complete units and unit public interfaces.
     * Returns false if the replacement could not be done, e.g. because 'oldUnit' is not a child of this object.
     * @param oldUnit
     * @param newUnit
     */
    replaceUnit(oldUnit: FreModelUnit, newUnit: FreModelUnit): boolean {
        if (oldUnit.freLanguageConcept() !== newUnit.freLanguageConcept()) {
            return false;
        }
        if (oldUnit.freOwnerDescriptor().owner !== this) {
            return false;
        }
        // we must store the interface in the same place as the unused unit, which info is held in FreContainer()
        if (oldUnit.freLanguageConcept() === "Topic" && oldUnit.freOwnerDescriptor().propertyName === "topic") {
            AST.changeNamed("removeUnit", () => {
                const index = this.topic.indexOf(oldUnit as Topic);
                this.topic.splice(index, 1, newUnit as Topic);
            });
        } else if (oldUnit.freLanguageConcept() === "Flow" && oldUnit.freOwnerDescriptor().propertyName === "flow") {
            AST.changeNamed("removeUnit", () => {
                const index = this.flow.indexOf(oldUnit as Flow);
                this.flow.splice(index, 1, newUnit as Flow);
            });
        } else if (oldUnit.freLanguageConcept() === "Test" && oldUnit.freOwnerDescriptor().propertyName === "tests") {
            AST.changeNamed("removeUnit", () => {
                const index = this.tests.indexOf(oldUnit as Test);
                this.tests.splice(index, 1, newUnit as Test);
            });
        } else {
            return false;
        }
        return true;
    }

    /**
     * Adds a model unit. Returns false if anything goes wrong.
     *
     * @param newUnit
     */
    addUnit(newUnit: FreModelUnit): boolean {
        if (!!newUnit) {
            const myMetatype = newUnit.freLanguageConcept();
            switch (myMetatype) {
                case "Topic": {
                    AST.changeNamed("addUnit", () => {
                        this.topic.push(newUnit as Topic);
                    });
                    return true;
                }
                case "Flow": {
                    AST.changeNamed("addUnit", () => {
                        this.flow.push(newUnit as Flow);
                    });
                    return true;
                }
                case "Test": {
                    AST.changeNamed("addUnit", () => {
                        this.tests.push(newUnit as Test);
                    });
                    return true;
                }
            }
        }
        return false;
    }

    /**
     * Removes a model unit. Returns false if anything goes wrong.
     *
     * @param oldUnit
     */
    removeUnit(oldUnit: FreModelUnit): boolean {
        if (!!oldUnit) {
            const myMetatype = oldUnit.freLanguageConcept();
            switch (myMetatype) {
                case "Topic": {
                    AST.changeNamed("removeUnit", () => {
                        this.topic.splice(this.topic.indexOf(oldUnit as Topic), 1);
                    });
                    return true;
                }
                case "Flow": {
                    AST.changeNamed("removeUnit", () => {
                        this.flow.splice(this.flow.indexOf(oldUnit as Flow), 1);
                    });
                    return true;
                }
                case "Test": {
                    AST.changeNamed("removeUnit", () => {
                        this.tests.splice(this.tests.indexOf(oldUnit as Test), 1);
                    });
                    return true;
                }
            }
        }
        return false;
    }

    /**
     * Returns an empty model unit of type 'typeName' within 'model'.
     *
     * @param typename
     */
    newUnit(typename: string): FreModelUnit {
        switch (typename) {
            case "Topic": {
                const unit: Topic = Topic.create({});
                AST.changeNamed("newUnit", () => {
                    this.topic.push(unit as Topic);
                });
                return unit;
            }
            case "Flow": {
                const unit: Flow = Flow.create({});
                AST.changeNamed("newUnit", () => {
                    this.flow.push(unit as Flow);
                });
                return unit;
            }
            case "Test": {
                const unit: Test = Test.create({});
                AST.changeNamed("newUnit", () => {
                    this.tests.push(unit as Test);
                });
                return unit;
            }
        }
        return null;
    }

    /**
     * Returns a list of model units.
     */
    getUnits(): FreModelUnit[] {
        let result: FreModelUnit[] = [];
        result = result.concat(this.topic);
        result = result.concat(this.flow);
        result = result.concat(this.tests);
        return result;
    }

    /**
     * Returns a list of model units of type 'type'.
     */
    getUnitsForType(type: string): FreModelUnit[] {
        switch (type) {
            case "Topic": {
                return this.topic;
            }
            case "Flow": {
                return this.flow;
            }
            case "Test": {
                return this.tests;
            }
        }
        return [];
    }
}
