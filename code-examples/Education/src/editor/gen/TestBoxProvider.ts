// Generated by the Freon Language Generator.
import { Box, BoxUtil, FreBoxProvider, FreProjectionHandler, BoxFactory, FreNodeReference } from "@freon4dsl/core";

import { Test, Topic, Flow } from "../../language/gen/index.js";

import { EducationEnvironment } from "../../config/gen/EducationEnvironment.js";

/**
 * This class implements the box provider for a single node of type Test.
 * The box provider is able to create the (tree of) boxes for the node, based
 * on the projections that are currently selected by the user.
 * The top of the tree of boxes is always a box of type ElementBox, which is
 * a box that will never be rendered itself, only its content will. Thus, we
 * have a stable entry in the complete box tree for every FreNode node.
 */
export class TestBoxProvider extends FreBoxProvider {
    constructor(mainHandler: FreProjectionHandler) {
        super(mainHandler);
        this.knownBoxProjections = ["default"];
        this.knownTableProjections = ["default"];
        this.conceptName = "Test";
    }

    protected getContent(projectionName: string): Box {
        // console.log("GET CONTENT " + this._node?.freId() + ' ' +  this._node?.freLanguageConcept() + ' ' + projectionName);
        // see if we need to use a custom projection
        if (!this.knownBoxProjections.includes(projectionName) && !this.knownTableProjections.includes(projectionName)) {
            const BOX: Box = this.mainHandler.executeCustomProjection(this._node, projectionName);
            if (!!BOX) {
                // found one, so return it
                return BOX;
            }
        } else {
            // select the box to return based on the projectionName
            if (projectionName === "default") {
                return this.getDefault();
            }
        }
        // in all other cases, return the default
        return this.getDefault();
    }

    private getDefault(): Box {
        return BoxFactory.verticalLayout(this._node as Test, "Test-overall", "", [
            BoxFactory.horizontalLayout(
                this._node as Test,
                "Test-hlist-line-0",
                "",
                [
                    BoxUtil.labelBox(this._node as Test, "Test", "top-1-line-0-item-0"),
                    BoxUtil.textBox(this._node as Test, "name"),
                    BoxUtil.labelBox(this._node as Test, "{", "top-1-line-0-item-2"),
                ],
                { selectable: false },
            ),
            BoxUtil.indentBox(this._node as Test, 4, "1", BoxUtil.labelBox(this._node as Test, "scenarios", "top-1-line-1-item-0")),
            BoxUtil.indentBox(
                this._node as Test,
                8,
                "2",
                BoxUtil.verticalPartListBox(this._node as Test, (this._node as Test).scenarios, "scenarios", null, this.mainHandler),
            ),
            BoxUtil.indentBox(
                this._node as Test,
                4,
                "3",
                BoxFactory.horizontalLayout(
                    this._node as Test,
                    "Test-hlist-line-3",
                    "",
                    [
                        BoxUtil.labelBox(this._node as Test, "topic", "top-1-line-3-item-0"),
                        BoxUtil.referenceBox(
                            this._node as Test,
                            "topic",
                            (selected: string) => {
                                (this._node as Test).topic = FreNodeReference.create<Topic>(selected, "Topic");
                            },
                            EducationEnvironment.getInstance().scoper,
                        ),
                    ],
                    { selectable: false },
                ),
            ),
            BoxUtil.indentBox(
                this._node as Test,
                4,
                "4",
                BoxFactory.horizontalLayout(
                    this._node as Test,
                    "Test-hlist-line-4",
                    "",
                    [
                        BoxUtil.labelBox(this._node as Test, "flow", "top-1-line-4-item-0"),
                        BoxUtil.referenceBox(
                            this._node as Test,
                            "flow",
                            (selected: string) => {
                                (this._node as Test).flow = FreNodeReference.create<Flow>(selected, "Flow");
                            },
                            EducationEnvironment.getInstance().scoper,
                        ),
                    ],
                    { selectable: false },
                ),
            ),
            BoxUtil.labelBox(this._node as Test, "}", "top-1-line-5-item-0"),
        ]);
    }
}
