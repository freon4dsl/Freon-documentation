// Generated by the Freon Language Generator.
import { observablepart, FreExpressionNode, FreParseLocation, FreNodeReference } from "@freon4dsl/core";
import { Exp, NumberLiteral, UnitKind } from "./internal.js";

/**
 * Class UnitLiteral is the implementation of the concept with the same name in the language definition file.
 * It uses mobx decorators to enable parts of the language environment, e.g. the editor, to react
 * to the changes in the state of its properties.
 */
export class UnitLiteral extends Exp implements FreExpressionNode {
    /**
     * A convenience method that creates an instance of this class
     * based on the properties defined in 'data'.
     * @param data partial object
     */
    static create(data: Partial<UnitLiteral>): UnitLiteral {
        const result = new UnitLiteral(data.$id);
        if (!!data.inner) {
            result.inner = data.inner;
        } else {
            result.inner = NumberLiteral.create({});
        }
        if (!!data.unit) {
            result.unit = data.unit;
        }
        if (!!data.parseLocation) {
            result.parseLocation = data.parseLocation;
        }
        return result;
    }

    readonly $typename: string = "UnitLiteral"; // holds the metatype in the form of a string

    parseLocation: FreParseLocation; // if relevant, the location of this element within the source from which it is parsed

    inner: NumberLiteral; // implementation of part 'inner'
    unit: FreNodeReference<UnitKind>; // implementation of reference 'unit'

    constructor(id?: string) {
        super(id);

        // Both 'observablepart' and 'observablepartlist' change the get and set of the attribute
        // such that the parent-part relationship is consistently maintained,
        // and make sure the part is observable. In lists no 'null' or 'undefined' values are allowed.
        observablepart(this, "inner");

        observablepart(this, "unit");
    }

    /**
     * Returns the metatype of this instance in the form of a string.
     */
    freLanguageConcept(): string {
        return this.$typename;
    }

    /**
     * Returns true if this instance is a model concept.
     */
    freIsModel(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is a model unit.
     */
    freIsUnit(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is an expression concept.
     */
    freIsExpression(): boolean {
        return true;
    }

    /**
     * Returns true if this instance is a binary expression concept.
     */
    freIsBinaryExpression(): boolean {
        return false;
    }
    /**
     * A convenience method that copies this instance into a new object.
     */
    copy(): UnitLiteral {
        const result = new UnitLiteral();
        if (!!this.inner) {
            result.inner = this.inner.copy();
        }
        if (!!this.unit) {
            result.unit = this.unit.copy();
        }
        return result;
    }
    /**
     * Matches a partial instance of this class to this object
     * based on the properties defined in the partial.
     * @param toBeMatched
     */
    public match(toBeMatched: Partial<UnitLiteral>): boolean {
        let result: boolean = super.match(toBeMatched);
        if (result && !!toBeMatched.inner) {
            result = result && this.inner.match(toBeMatched.inner);
        }
        if (result && !!toBeMatched.unit) {
            result = result && this.unit.match(toBeMatched.unit);
        }
        return result;
    }

    /**
     * Convenience method for reference 'unit'.
     * Instead of returning a 'FreNodeReference<UnitKind>' object,
     * it returns the referred 'UnitKind' object, if it can be found.
     */
    get $unit(): UnitKind {
        if (!!this.unit) {
            return this.unit.referred;
        }
        return null;
    }
}
