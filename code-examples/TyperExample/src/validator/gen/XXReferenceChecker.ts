// Generated by the Freon Language Generator.
import { FreError, FreErrorSeverity, FreWriter, FreNodeReference, FreNamedNode, FreNode, FreLanguageEnvironment } from "@freon4dsl/core";
import { TypeRef, GenericType, UnitOfMeasurement, UnitLiteral, GenericLiteral } from "../../language/gen/index.js";
import { XXDefaultWorker } from "../../utils/gen/index.js";
import { XXCheckerInterface } from "./XXValidator.js";

/**
 * Class XXReferenceChecker is part of the implementation of the default validator.
 * It checks whether references can be found within the model.
 *
 * Class XXWalker implements the traversal of the model tree. This class implements
 * the actual checking of each node in the tree.
 */
export class XXReferenceChecker extends XXDefaultWorker implements XXCheckerInterface {
    // 'myWriter' is used to provide error messages on the nodes in the model tree
    myWriter: FreWriter = FreLanguageEnvironment.getInstance().writer;
    // 'errorList' holds the errors found while traversing the model tree
    errorList: FreError[] = [];
    private refSeparator: string = "/";

    /**
     * Checks 'modelelement' before checking its children.
     * Found errors are pushed onto 'errorlist'.
     * If an error is found, it is considered 'fatal', which means that no other checks on
     * 'modelelement' are performed.
     *
     * @param modelelement
     */
    public execBeforeTypeRef(modelelement: TypeRef): boolean {
        let hasFatalError: boolean = false;
        if (!!modelelement.type && modelelement.type.referred === null) {
            this.makeErrorMessage(modelelement, modelelement.type, "type", `${"unnamed"}`);
            hasFatalError = true;
        }
        return hasFatalError;
    }

    /**
     * Checks 'modelelement' before checking its children.
     * Found errors are pushed onto 'errorlist'.
     * If an error is found, it is considered 'fatal', which means that no other checks on
     * 'modelelement' are performed.
     *
     * @param modelelement
     */
    public execBeforeGenericType(modelelement: GenericType): boolean {
        let hasFatalError: boolean = false;
        if (!!modelelement.kind && modelelement.kind.referred === null) {
            this.makeErrorMessage(modelelement, modelelement.kind, "kind", `${"unnamed"}`);
            hasFatalError = true;
        }
        return hasFatalError;
    }

    /**
     * Checks 'modelelement' before checking its children.
     * Found errors are pushed onto 'errorlist'.
     * If an error is found, it is considered 'fatal', which means that no other checks on
     * 'modelelement' are performed.
     *
     * @param modelelement
     */
    public execBeforeUnitOfMeasurement(modelelement: UnitOfMeasurement): boolean {
        let hasFatalError: boolean = false;
        if (!!modelelement.baseType && modelelement.baseType.referred === null) {
            this.makeErrorMessage(modelelement, modelelement.baseType, "baseType", `${"unnamed"}`);
            hasFatalError = true;
        }
        if (!!modelelement.unit && modelelement.unit.referred === null) {
            this.makeErrorMessage(modelelement, modelelement.unit, "unit", `${"unnamed"}`);
            hasFatalError = true;
        }
        return hasFatalError;
    }

    /**
     * Checks 'modelelement' before checking its children.
     * Found errors are pushed onto 'errorlist'.
     * If an error is found, it is considered 'fatal', which means that no other checks on
     * 'modelelement' are performed.
     *
     * @param modelelement
     */
    public execBeforeUnitLiteral(modelelement: UnitLiteral): boolean {
        let hasFatalError: boolean = false;
        if (!!modelelement.unit && modelelement.unit.referred === null) {
            this.makeErrorMessage(modelelement, modelelement.unit, "unit", `${"unnamed"}`);
            hasFatalError = true;
        }
        return hasFatalError;
    }

    /**
     * Checks 'modelelement' before checking its children.
     * Found errors are pushed onto 'errorlist'.
     * If an error is found, it is considered 'fatal', which means that no other checks on
     * 'modelelement' are performed.
     *
     * @param modelelement
     */
    public execBeforeGenericLiteral(modelelement: GenericLiteral): boolean {
        let hasFatalError: boolean = false;
        if (!!modelelement.kind && modelelement.kind.referred === null) {
            this.makeErrorMessage(modelelement, modelelement.kind, "kind", `${"unnamed"}`);
            hasFatalError = true;
        }
        return hasFatalError;
    }

    private makeErrorMessage(
        modelelement: FreNode,
        referredElem: FreNodeReference<FreNamedNode>,
        propertyName: string,
        locationDescription: string
    ) {
        const scoper = FreLanguageEnvironment.getInstance().scoper;
        const possibles = scoper.getVisibleElements(modelelement).filter((elem) => elem.name === referredElem.name);
        if (possibles.length > 0) {
            this.errorList.push(
                new FreError(
                    `Reference '${referredElem.pathnameToString(this.refSeparator)}' should have type '${
                        referredElem.typeName
                    }', but found type(s) [${possibles.map((elem) => `${elem.freLanguageConcept()}`).join(", ")}]`,
                    modelelement,
                    `${propertyName} of ${locationDescription}`,
                    `${propertyName}`,
                    FreErrorSeverity.Error
                )
            );
        } else {
            this.errorList.push(
                new FreError(
                    `Cannot find reference '${referredElem.pathnameToString(this.refSeparator)}'`,
                    modelelement,
                    `${propertyName} of ${locationDescription}`,
                    `${propertyName}`,
                    FreErrorSeverity.Error
                )
            );
        }
    }
}
