// Generated by the Freon Language Generator.
import {
    observablepart,
    observablepartlist,
    FreExpressionNode,
    FreParseLocation,
    FreNodeReference,
    matchElementList,
} from "@freon4dsl/core";
import { DocuExpression, CalcFunction } from "./internal.js";

/**
 * Class FunctionCallExpression is the implementation of the concept with the same name in the language definition file.
 * It uses mobx decorators to enable parts of the language environment, e.g. the editor, to react
 * to the changes in the state of its properties.
 */
export class FunctionCallExpression extends DocuExpression implements FreExpressionNode {
    /**
     * A convenience method that creates an instance of this class
     * based on the properties defined in 'data'.
     * @param data partial object
     */
    static create(data: Partial<FunctionCallExpression>): FunctionCallExpression {
        const result = new FunctionCallExpression(data.$id);
        if (!!data.args) {
            data.args.forEach((x) => result.args.push(x));
        }
        if (!!data.funcDefinition) {
            result.funcDefinition = data.funcDefinition;
        }
        if (!!data.parseLocation) {
            result.parseLocation = data.parseLocation;
        }
        return result;
    }

    readonly $typename: string = "FunctionCallExpression"; // holds the metatype in the form of a string

    parseLocation: FreParseLocation; // if relevant, the location of this element within the source from which it is parsed

    args: DocuExpression[]; // implementation of part 'args'
    funcDefinition: FreNodeReference<CalcFunction>; // implementation of reference 'funcDefinition'

    constructor(id?: string) {
        super(id);

        // Both 'observablepart' and 'observablepartlist' change the get and set of the attribute
        // such that the parent-part relationship is consistently maintained,
        // and make sure the part is observable. In lists no 'null' or 'undefined' values are allowed.
        observablepartlist(this, "args");
        observablepart(this, "funcDefinition");
    }

    /**
     * Returns the metatype of this instance in the form of a string.
     */
    freLanguageConcept(): string {
        return this.$typename;
    }

    /**
     * Returns true if this instance is a model concept.
     */
    freIsModel(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is a model unit.
     */
    freIsUnit(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is an expression concept.
     */
    freIsExpression(): boolean {
        return true;
    }

    /**
     * Returns true if this instance is a binary expression concept.
     */
    freIsBinaryExpression(): boolean {
        return false;
    }
    /**
     * A convenience method that copies this instance into a new object.
     */
    copy(): FunctionCallExpression {
        const result = new FunctionCallExpression();
        if (!!this.args) {
            this.args.forEach((x) => result.args.push(x.copy()));
        }
        if (!!this.funcDefinition) {
            result.funcDefinition = this.funcDefinition.copy();
        }
        return result;
    }
    /**
     * Matches a partial instance of this class to this object
     * based on the properties defined in the partial.
     * @param toBeMatched
     */
    public match(toBeMatched: Partial<FunctionCallExpression>): boolean {
        let result: boolean = super.match(toBeMatched);
        if (result && !!toBeMatched.args) {
            result = result && matchElementList(this.args, toBeMatched.args);
        }
        if (result && !!toBeMatched.funcDefinition) {
            result = result && this.funcDefinition.match(toBeMatched.funcDefinition);
        }
        return result;
    }

    /**
     * Convenience method for reference 'funcDefinition'.
     * Instead of returning a 'FreNodeReference<CalcFunction>' object,
     * it returns the referred 'CalcFunction' object, if it can be found.
     */
    get $funcDefinition(): CalcFunction {
        if (!!this.funcDefinition) {
            return this.funcDefinition.referred;
        }
        return null;
    }
}
