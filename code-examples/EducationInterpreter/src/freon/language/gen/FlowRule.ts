// Generated by the Freon Language Generator.
import {
    MobxModelElementImpl,
    observableprim,
    observablepart,
    observablepartlist,
    FreNamedNode,
    FreParseLocation,
    FreNodeReference,
    FreUtils,
    matchElementList,
} from "@freon4dsl/core";
import { PageTransition, Page } from "./internal.js";

import { makeObservable, action } from "mobx";

/**
 * Class FlowRule is the implementation of the concept with the same name in the language definition file.
 * It uses mobx decorators to enable parts of the language environment, e.g. the editor, to react
 * to the changes in the state of its properties.
 */
export class FlowRule extends MobxModelElementImpl implements FreNamedNode {
    /**
     * A convenience method that creates an instance of this class
     * based on the properties defined in 'data'.
     * @param data partial object
     */
    static create(data: Partial<FlowRule>): FlowRule {
        const result = new FlowRule(data.$id);
        if (!!data.name) {
            result.name = data.name;
        }
        if (!!data.description) {
            result.description = data.description;
        }
        if (!!data.transitions) {
            data.transitions.forEach((x) => result.transitions.push(x));
        }
        if (!!data.page) {
            result.page = data.page;
        }
        if (!!data.parseLocation) {
            result.parseLocation = data.parseLocation;
        }
        return result;
    }

    readonly $typename: string = "FlowRule"; // holds the metatype in the form of a string
    $id: string = ""; // a unique identifier
    parseLocation: FreParseLocation; // if relevant, the location of this element within the source from which it is parsed
    name: string; // implementation of name
    description: string; // implementation of description
    transitions: PageTransition[]; // implementation of part 'transitions'
    page: FreNodeReference<Page>; // implementation of reference 'page'

    constructor(id?: string) {
        super();
        if (!!id) {
            this.$id = id;
        } else {
            this.$id = FreUtils.ID(); // uuid.v4();
        }
        // Both 'observableprim' and 'observableprimlist' change the get and set of the attribute
        // such that the part is observable. In lists no 'null' or 'undefined' values are allowed.
        observableprim(this, "name");
        this.name = "";
        observableprim(this, "description");
        this.description = "";

        // Both 'observablepart' and 'observablepartlist' change the get and set of the attribute
        // such that the parent-part relationship is consistently maintained,
        // and make sure the part is observable. In lists no 'null' or 'undefined' values are allowed.
        observablepartlist(this, "transitions");
        observablepart(this, "page");

        // Make copy method a mobx action
        makeObservable(this, {
            copy: action,
        });
    }

    /**
     * Returns the metatype of this instance in the form of a string.
     */
    freLanguageConcept(): string {
        return this.$typename;
    }

    /**
     * Returns the unique identifier of this instance.
     */
    freId(): string {
        return this.$id;
    }

    /**
     * Returns true if this instance is a model concept.
     */
    freIsModel(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is a model unit.
     */
    freIsUnit(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is an expression concept.
     */
    freIsExpression(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is a binary expression concept.
     */
    freIsBinaryExpression(): boolean {
        return false;
    }
    /**
     * A convenience method that copies this instance into a new object.
     */
    copy(): FlowRule {
        const result = new FlowRule();
        if (!!this.name) {
            result.name = this.name;
        }
        if (!!this.description) {
            result.description = this.description;
        }
        if (!!this.transitions) {
            this.transitions.forEach((x) => result.transitions.push(x.copy()));
        }
        if (!!this.page) {
            result.page = this.page.copy();
        }
        return result;
    }
    /**
     * Matches a partial instance of this class to this object
     * based on the properties defined in the partial.
     * @param toBeMatched
     */
    public match(toBeMatched: Partial<FlowRule>): boolean {
        let result: boolean = true;
        if (result && toBeMatched.name !== null && toBeMatched.name !== undefined && toBeMatched.name.length > 0) {
            result = result && this.name === toBeMatched.name;
        }
        if (result && toBeMatched.description !== null && toBeMatched.description !== undefined && toBeMatched.description.length > 0) {
            result = result && this.description === toBeMatched.description;
        }
        if (result && !!toBeMatched.transitions) {
            result = result && matchElementList(this.transitions, toBeMatched.transitions);
        }
        if (result && !!toBeMatched.page) {
            result = result && this.page.match(toBeMatched.page);
        }
        return result;
    }

    /**
     * Convenience method for reference 'page'.
     * Instead of returning a 'FreNodeReference<Page>' object,
     * it returns the referred 'Page' object, if it can be found.
     */
    get $page(): Page {
        if (!!this.page) {
            return this.page.referred;
        }
        return null;
    }
}
