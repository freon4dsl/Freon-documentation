// Generated by the Freon Language Generator.
import { observableprim, FreNode, FreParseLocation } from "@freon4dsl/core";
import { NumberConcept } from "./internal.js";

/**
 * Class Fraction is the implementation of the concept with the same name in the language definition file.
 * It uses mobx decorators to enable parts of the language environment, e.g. the editor, to react
 * to the changes in the state of its properties.
 */
export class Fraction extends NumberConcept implements FreNode {
    /**
     * A convenience method that creates an instance of this class
     * based on the properties defined in 'data'.
     * @param data partial object
     */
    static create(data: Partial<Fraction>): Fraction {
        const result = new Fraction(data.$id);
        if (!!data.numerator) {
            result.numerator = data.numerator;
        }
        if (!!data.denominator) {
            result.denominator = data.denominator;
        }
        if (!!data.parseLocation) {
            result.parseLocation = data.parseLocation;
        }
        return result;
    }

    readonly $typename: string = "Fraction"; // holds the metatype in the form of a string

    parseLocation: FreParseLocation; // if relevant, the location of this element within the source from which it is parsed
    numerator: number; // implementation of numerator
    denominator: number; // implementation of denominator

    constructor(id?: string) {
        super(id);
        // Both 'observableprim' and 'observableprimlist' change the get and set of the attribute
        // such that the part is observable. In lists no 'null' or 'undefined' values are allowed.
        observableprim(this, "numerator");
        this.numerator = 0;
        observableprim(this, "denominator");
        this.denominator = 0;
    }

    /**
     * Returns the metatype of this instance in the form of a string.
     */
    freLanguageConcept(): string {
        return this.$typename;
    }

    /**
     * Returns true if this instance is a model concept.
     */
    freIsModel(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is a model unit.
     */
    freIsUnit(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is an expression concept.
     */
    freIsExpression(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is a binary expression concept.
     */
    freIsBinaryExpression(): boolean {
        return false;
    }
    /**
     * A convenience method that copies this instance into a new object.
     */
    copy(): Fraction {
        const result = new Fraction();
        if (!!this.numerator) {
            result.numerator = this.numerator;
        }
        if (!!this.denominator) {
            result.denominator = this.denominator;
        }
        return result;
    }
    /**
     * Matches a partial instance of this class to this object
     * based on the properties defined in the partial.
     * @param toBeMatched
     */
    public match(toBeMatched: Partial<Fraction>): boolean {
        let result: boolean = super.match(toBeMatched);
        if (result && toBeMatched.numerator !== null && toBeMatched.numerator !== undefined) {
            result = result && this.numerator === toBeMatched.numerator;
        }
        if (result && toBeMatched.denominator !== null && toBeMatched.denominator !== undefined) {
            result = result && this.denominator === toBeMatched.denominator;
        }
        return result;
    }
}
