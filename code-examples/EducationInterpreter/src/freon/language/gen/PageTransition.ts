// Generated by the Freon Language Generator.
import { MobxModelElementImpl, observablepart, FreNode, FreParseLocation, FreNodeReference, FreUtils } from "@freon4dsl/core";
import { Grade, Page } from "./internal.js";

import { makeObservable, action } from "mobx";

/**
 * Class PageTransition is the implementation of the concept with the same name in the language definition file.
 * It uses mobx decorators to enable parts of the language environment, e.g. the editor, to react
 * to the changes in the state of its properties.
 */
export class PageTransition extends MobxModelElementImpl implements FreNode {
    /**
     * A convenience method that creates an instance of this class
     * based on the properties defined in 'data'.
     * @param data partial object
     */
    static create(data: Partial<PageTransition>): PageTransition {
        const result = new PageTransition(data.$id);
        if (!!data.condition) {
            result.condition = data.condition;
        }
        if (!!data.toPage) {
            result.toPage = data.toPage;
        }
        if (!!data.parseLocation) {
            result.parseLocation = data.parseLocation;
        }
        return result;
    }

    readonly $typename: string = "PageTransition"; // holds the metatype in the form of a string
    $id: string = ""; // a unique identifier
    parseLocation: FreParseLocation; // if relevant, the location of this element within the source from which it is parsed

    condition: FreNodeReference<Grade>; // implementation of reference 'condition'
    toPage: FreNodeReference<Page>; // implementation of reference 'toPage'

    constructor(id?: string) {
        super();
        if (!!id) {
            this.$id = id;
        } else {
            this.$id = FreUtils.ID(); // uuid.v4();
        }

        // Both 'observablepart' and 'observablepartlist' change the get and set of the attribute
        // such that the parent-part relationship is consistently maintained,
        // and make sure the part is observable. In lists no 'null' or 'undefined' values are allowed.
        observablepart(this, "condition");

        observablepart(this, "toPage");

        // Make copy method a mobx action
        makeObservable(this, {
            copy: action,
        });
    }

    /**
     * Returns the metatype of this instance in the form of a string.
     */
    freLanguageConcept(): string {
        return this.$typename;
    }

    /**
     * Returns the unique identifier of this instance.
     */
    freId(): string {
        return this.$id;
    }

    /**
     * Returns true if this instance is a model concept.
     */
    freIsModel(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is a model unit.
     */
    freIsUnit(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is an expression concept.
     */
    freIsExpression(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is a binary expression concept.
     */
    freIsBinaryExpression(): boolean {
        return false;
    }
    /**
     * A convenience method that copies this instance into a new object.
     */
    copy(): PageTransition {
        const result = new PageTransition();
        if (!!this.condition) {
            result.condition = this.condition.copy();
        }
        if (!!this.toPage) {
            result.toPage = this.toPage.copy();
        }
        return result;
    }
    /**
     * Matches a partial instance of this class to this object
     * based on the properties defined in the partial.
     * @param toBeMatched
     */
    public match(toBeMatched: Partial<PageTransition>): boolean {
        let result: boolean = true;
        if (result && !!toBeMatched.condition) {
            result = result && this.condition.match(toBeMatched.condition);
        }
        if (result && !!toBeMatched.toPage) {
            result = result && this.toPage.match(toBeMatched.toPage);
        }
        return result;
    }

    /**
     * Convenience method for reference 'condition'.
     * Instead of returning a 'FreNodeReference<Grade>' object,
     * it returns the referred 'Grade' object, if it can be found.
     */
    get $condition(): Grade {
        if (!!this.condition) {
            return this.condition.referred;
        }
        return null;
    }
    /**
     * Convenience method for reference 'toPage'.
     * Instead of returning a 'FreNodeReference<Page>' object,
     * it returns the referred 'Page' object, if it can be found.
     */
    get $toPage(): Page {
        if (!!this.toPage) {
            return this.toPage.referred;
        }
        return null;
    }
}
