// Generated by the Freon Language Generator.
import { MobxModelElementImpl, observablepart, FreNode, FreParseLocation, FreNodeReference, FreUtils } from "@freon4dsl/core";
import { NumberConcept, Question } from "./internal.js";

import { makeObservable, action } from "mobx";

/**
 * Class Answer is the implementation of the concept with the same name in the language definition file.
 * It uses mobx decorators to enable parts of the language environment, e.g. the editor, to react
 * to the changes in the state of its properties.
 */
export class Answer extends MobxModelElementImpl implements FreNode {
    /**
     * A convenience method that creates an instance of this class
     * based on the properties defined in 'data'.
     * @param data partial object
     */
    static create(data: Partial<Answer>): Answer {
        const result = new Answer(data.$id);
        if (!!data.value) {
            result.value = data.value;
        }
        if (!!data.question) {
            result.question = data.question;
        }
        if (!!data.parseLocation) {
            result.parseLocation = data.parseLocation;
        }
        return result;
    }

    readonly $typename: string = "Answer"; // holds the metatype in the form of a string
    $id: string = ""; // a unique identifier
    parseLocation: FreParseLocation; // if relevant, the location of this element within the source from which it is parsed

    value: NumberConcept; // implementation of part 'value'
    question: FreNodeReference<Question>; // implementation of reference 'question'

    constructor(id?: string) {
        super();
        if (!!id) {
            this.$id = id;
        } else {
            this.$id = FreUtils.ID(); // uuid.v4();
        }

        // Both 'observablepart' and 'observablepartlist' change the get and set of the attribute
        // such that the parent-part relationship is consistently maintained,
        // and make sure the part is observable. In lists no 'null' or 'undefined' values are allowed.
        observablepart(this, "value");

        observablepart(this, "question");

        // Make copy method a mobx action
        makeObservable(this, {
            copy: action,
        });
    }

    /**
     * Returns the metatype of this instance in the form of a string.
     */
    freLanguageConcept(): string {
        return this.$typename;
    }

    /**
     * Returns the unique identifier of this instance.
     */
    freId(): string {
        return this.$id;
    }

    /**
     * Returns true if this instance is a model concept.
     */
    freIsModel(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is a model unit.
     */
    freIsUnit(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is an expression concept.
     */
    freIsExpression(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is a binary expression concept.
     */
    freIsBinaryExpression(): boolean {
        return false;
    }
    /**
     * A convenience method that copies this instance into a new object.
     */
    copy(): Answer {
        const result = new Answer();
        if (!!this.value) {
            result.value = this.value.copy();
        }
        if (!!this.question) {
            result.question = this.question.copy();
        }
        return result;
    }
    /**
     * Matches a partial instance of this class to this object
     * based on the properties defined in the partial.
     * @param toBeMatched
     */
    public match(toBeMatched: Partial<Answer>): boolean {
        let result: boolean = true;
        if (result && !!toBeMatched.value) {
            result = result && this.value.match(toBeMatched.value);
        }
        if (result && !!toBeMatched.question) {
            result = result && this.question.match(toBeMatched.question);
        }
        return result;
    }

    /**
     * Convenience method for reference 'question'.
     * Instead of returning a 'FreNodeReference<Question>' object,
     * it returns the referred 'Question' object, if it can be found.
     */
    get $question(): Question {
        if (!!this.question) {
            return this.question.referred;
        }
        return null;
    }
}
