// Generated by the Freon Language Generator.
import { FreLanguage, FreScoperComposite } from "@freon4dsl/core";
import { freonConfiguration } from "../../config/FreonConfiguration.js";
import { EducationScoper } from "./EducationScoper.js";

/**
 * Adds all known scopers to the main scoper.
 * @param rootScoper
 */
export function initializeScopers(rootScoper: FreScoperComposite) {
    for (const p of freonConfiguration.customScopers) {
        rootScoper.appendScoper(p);
    }
    rootScoper.appendScoper(new EducationScoper());
}

/**
 * Adds namespace info to the in-memory representation of the language metamodel.
 */
export function initializeScoperDef(rootScoper: FreScoperComposite) {
    FreLanguage.getInstance().classifier("Page").isNamespace = true;
    FreLanguage.getInstance().classifier("Step").isNamespace = true;
    FreLanguage.getInstance().classifier("Theory").isNamespace = true;
    FreLanguage.getInstance().classifier("Video").isNamespace = true;
    FreLanguage.getInstance().classifier("WorkSheet").isNamespace = true;
    FreLanguage.getInstance().classifier("ExamplePage").isNamespace = true;
    FreLanguage.getInstance().classifier("InDepthMaterial").isNamespace = true;
    FreLanguage.getInstance().classifier("LastStep").isNamespace = true;
    initializeScopers(rootScoper);
}
