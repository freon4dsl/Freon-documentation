// Generated by the Freon Language Generator.
import { observablepart, FreNode, FreParseLocation, FreNodeReference } from "@freon4dsl/core";
import { DocuType, GenericKind } from "./internal.js";

/**
 * Class GenericTypeDecl is the implementation of the concept with the same name in the language definition file.
 * It uses mobx decorators to enable parts of the language environment, e.g. the editor, to react
 * to the changes in the state of its properties.
 */
export class GenericTypeDecl extends DocuType implements FreNode {
    /**
     * A convenience method that creates an instance of this class
     * based on the properties defined in 'data'.
     * @param data partial object
     */
    static create(data: Partial<GenericTypeDecl>): GenericTypeDecl {
        const result = new GenericTypeDecl(data.$id);
        if (!!data.baseType) {
            result.baseType = data.baseType;
        }
        if (!!data.kind) {
            result.kind = data.kind;
        }
        if (!!data.parseLocation) {
            result.parseLocation = data.parseLocation;
        }
        return result;
    }

    readonly $typename: string = "GenericTypeDecl"; // holds the metatype in the form of a string

    parseLocation: FreParseLocation; // if relevant, the location of this element within the source from which it is parsed

    baseType: DocuType; // implementation of part 'baseType'
    kind: FreNodeReference<GenericKind>; // implementation of reference 'kind'

    constructor(id?: string) {
        super(id);

        // Both 'observablepart' and 'observablepartlist' change the get and set of the attribute
        // such that the parent-part relationship is consistently maintained,
        // and make sure the part is observable. In lists no 'null' or 'undefined' values are allowed.
        observablepart(this, "baseType");

        observablepart(this, "kind");
    }

    /**
     * Returns the metatype of this instance in the form of a string.
     */
    freLanguageConcept(): string {
        return this.$typename;
    }

    /**
     * Returns true if this instance is a model concept.
     */
    freIsModel(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is a model unit.
     */
    freIsUnit(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is an expression concept.
     */
    freIsExpression(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is a binary expression concept.
     */
    freIsBinaryExpression(): boolean {
        return false;
    }
    /**
     * A convenience method that copies this instance into a new object.
     */
    copy(): GenericTypeDecl {
        const result = new GenericTypeDecl();
        if (!!this.baseType) {
            result.baseType = this.baseType.copy();
        }
        if (!!this.kind) {
            result.kind = this.kind.copy();
        }
        return result;
    }
    /**
     * Matches a partial instance of this class to this object
     * based on the properties defined in the partial.
     * @param toBeMatched
     */
    public match(toBeMatched: Partial<GenericTypeDecl>): boolean {
        let result: boolean = super.match(toBeMatched);
        if (result && !!toBeMatched.baseType) {
            result = result && this.baseType.match(toBeMatched.baseType);
        }
        if (result && !!toBeMatched.kind) {
            result = result && this.kind.match(toBeMatched.kind);
        }
        return result;
    }

    /**
     * Convenience method for reference 'kind'.
     * Instead of returning a 'FreNodeReference<GenericKind>' object,
     * it returns the referred 'GenericKind' object, if it can be found.
     */
    get $kind(): GenericKind {
        if (!!this.kind) {
            return this.kind.referred;
        }
        return null;
    }
}
