// Generated by the Freon Language Generator.
import {
    FreModelUnit,
    MobxModelElementImpl,
    observableprim,
    observablepartlist,
    FreModel,
    FreLanguage,
    FreParseLocation,
    AST,
    FreUtils,
    matchElementList,
} from "@freon4dsl/core";
import { Part, Product } from "./internal.js";

/**
 * Class InsuranceModel is the implementation of the model with the same name in the language definition file.
 * It uses mobx decorators to enable parts of the language environment, e.g. the editor, to react
 * to any changes in the state of its properties.
 */
export class InsuranceModel extends MobxModelElementImpl implements FreModel {
    /**
     * A convenience method that creates an instance of this class
     * based on the properties defined in 'data'.
     * @param data partial object
     */
    static create(data: Partial<InsuranceModel>): InsuranceModel {
        const result = new InsuranceModel(data.$id);
        if (!!data.name) {
            result.name = data.name;
        }
        if (!!data.parts) {
            data.parts.forEach((x) => result.parts.push(x));
        }
        if (!!data.products) {
            data.products.forEach((x) => result.products.push(x));
        }
        if (!!data.parseLocation) {
            result.parseLocation = data.parseLocation;
        }
        return result;
    }

    readonly $typename: string = "InsuranceModel"; // holds the metatype in the form of a string
    $id: string = ""; // a unique identifier
    parseLocation: FreParseLocation; // if relevant, the location of this element within the source from which it is parsed
    name: string; // implementation of name
    parts: Part[]; // implementation of part 'parts'
    products: Product[]; // implementation of part 'products'

    constructor(id?: string) {
        super();
        if (!!id) {
            this.$id = id;
        } else {
            this.$id = FreUtils.ID(); // uuid.v4();
        }

        // Both 'observablepart' and 'observablepartlist' change the get and set of the attribute
        // such that the parent-part relationship is consistently maintained,
        // and make sure the part is observable. In lists no 'null' or 'undefined' values are allowed.
        observablepartlist(this, "parts");
        observablepartlist(this, "products");
    }

    /**
     * Returns the metatype of this instance in the form of a string.
     */
    freLanguageConcept(): string {
        return this.$typename;
    }

    /**
     * Returns the unique identifier of this instance.
     */
    freId(): string {
        return this.$id;
    }

    /**
     * Returns true if this instance is a model concept.
     */
    freIsModel(): boolean {
        return true;
    }

    /**
     * Returns true if this instance is a model unit.
     */
    freIsUnit(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is an expression concept.
     */
    freIsExpression(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is a binary expression concept.
     */
    freIsBinaryExpression(): boolean {
        return false;
    }
    /**
     * A convenience method that copies this instance into a new object.
     */
    copy(): InsuranceModel {
        const result = new InsuranceModel();
        if (!!this.name) {
            result.name = this.name;
        }
        if (!!this.parts) {
            this.parts.forEach((x) => result.parts.push(x.copy()));
        }
        if (!!this.products) {
            this.products.forEach((x) => result.products.push(x.copy()));
        }
        return result;
    }
    /**
     * Matches a partial instance of this class to this object
     * based on the properties defined in the partial.
     * @param toBeMatched
     */
    public match(toBeMatched: Partial<InsuranceModel>): boolean {
        let result: boolean = true;
        if (result && toBeMatched.name !== null && toBeMatched.name !== undefined && toBeMatched.name.length > 0) {
            result = result && this.name === toBeMatched.name;
        }
        if (result && !!toBeMatched.parts) {
            result = result && matchElementList(this.parts, toBeMatched.parts);
        }
        if (result && !!toBeMatched.products) {
            result = result && matchElementList(this.products, toBeMatched.products);
        }
        return result;
    }

    /**
     * A convenience method that finds a unit of this model based on its name and 'metatype'.
     * @param name
     * @param metatype
     */
    findUnit(name: string, metatype?: string): FreModelUnit {
        let result: FreModelUnit = null;
        const checkType = metatype !== undefined;
        result = this.getUnits().find(
            (mod) => mod.name === name && (checkType ? FreLanguage.getInstance().metaConformsToType(mod, metatype) : true),
        );
        if (!!result) {
            return result;
        }
        return null;
    }

    /**
     * Replaces a model unit by a new one. Used for swapping between complete units and unit public interfaces.
     * Returns false if the replacement could not be done, e.g. because 'oldUnit' is not a child of this object.
     * @param oldUnit
     * @param newUnit
     */
    replaceUnit(oldUnit: FreModelUnit, newUnit: FreModelUnit): boolean {
        if (oldUnit.freLanguageConcept() !== newUnit.freLanguageConcept()) {
            return false;
        }
        if (oldUnit.freOwnerDescriptor().owner !== this) {
            return false;
        }
        // we must store the interface in the same place as the old unit, which info is held in FreContainer()
        if (oldUnit.freLanguageConcept() === "Part" && oldUnit.freOwnerDescriptor().propertyName === "parts") {
            AST.changeNamed("removeUnit", () => {
                const index = this.parts.indexOf(oldUnit as Part);
                this.parts.splice(index, 1, newUnit as Part);
            });
        } else if (oldUnit.freLanguageConcept() === "Product" && oldUnit.freOwnerDescriptor().propertyName === "products") {
            AST.changeNamed("removeUnit", () => {
                const index = this.products.indexOf(oldUnit as Product);
                this.products.splice(index, 1, newUnit as Product);
            });
        } else {
            return false;
        }
        return true;
    }

    /**
     * Adds a model unit. Returns false if anything goes wrong.
     *
     * @param newUnit
     */
    addUnit(newUnit: FreModelUnit): boolean {
        if (!!newUnit) {
            const myMetatype = newUnit.freLanguageConcept();
            switch (myMetatype) {
                case "Part": {
                    AST.changeNamed("addUnit", () => {
                        this.parts.push(newUnit as Part);
                    });
                    return true;
                }
                case "Product": {
                    AST.changeNamed("addUnit", () => {
                        this.products.push(newUnit as Product);
                    });
                    return true;
                }
            }
        }
        return false;
    }

    /**
     * Removes a model unit. Returns false if anything goes wrong.
     *
     * @param oldUnit
     */
    removeUnit(oldUnit: FreModelUnit): boolean {
        if (!!oldUnit) {
            const myMetatype = oldUnit.freLanguageConcept();
            switch (myMetatype) {
                case "Part": {
                    AST.changeNamed("removeUnit", () => {
                        this.parts.splice(this.parts.indexOf(oldUnit as Part), 1);
                    });
                    return true;
                }
                case "Product": {
                    AST.changeNamed("removeUnit", () => {
                        this.products.splice(this.products.indexOf(oldUnit as Product), 1);
                    });
                    return true;
                }
            }
        }
        return false;
    }

    /**
     * Returns an empty model unit of type 'typeName' within 'model'.
     *
     * @param typename
     */
    newUnit(typename: string): FreModelUnit {
        switch (typename) {
            case "Part": {
                const unit: Part = Part.create({});
                AST.changeNamed("newUnit", () => {
                    this.parts.push(unit as Part);
                });
                return unit;
            }
            case "Product": {
                const unit: Product = Product.create({});
                AST.changeNamed("newUnit", () => {
                    this.products.push(unit as Product);
                });
                return unit;
            }
        }
        return null;
    }

    /**
     * Returns a list of model units.
     */
    getUnits(): FreModelUnit[] {
        let result: FreModelUnit[] = [];
        result = result.concat(this.parts);
        result = result.concat(this.products);
        return result;
    }

    /**
     * Returns a list of model units of type 'type'.
     */
    getUnitsForType(type: string): FreModelUnit[] {
        switch (type) {
            case "Part": {
                return this.parts;
            }
            case "Product": {
                return this.products;
            }
        }
        return [];
    }
}
