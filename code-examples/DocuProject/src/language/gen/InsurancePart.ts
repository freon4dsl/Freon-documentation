// Generated by the Freon Language Generator.
import { MobxModelElementImpl, observableprim, observablepart, FreNamedNode, FreParseLocation, FreUtils } from "@freon4dsl/core";
import { PercentageLiteral, EuroLiteral } from "./internal.js";

/**
 * Class InsurancePart is the implementation of the concept with the same name in the language definition file.
 * It uses mobx decorators to enable parts of the language environment, e.g. the editor, to react
 * to the changes in the state of its properties.
 */
export class InsurancePart extends MobxModelElementImpl implements FreNamedNode {
    /**
     * A convenience method that creates an instance of this class
     * based on the properties defined in 'data'.
     * @param data partial object
     */
    static create(data: Partial<InsurancePart>): InsurancePart {
        const result = new InsurancePart(data.$id);
        if (!!data.name) {
            result.name = data.name;
        }
        if (!!data.isApproved) {
            result.isApproved = data.isApproved;
        }
        if (!!data.statisticalRisk) {
            result.statisticalRisk = data.statisticalRisk;
        } else {
            result.statisticalRisk = PercentageLiteral.create({});
        }
        if (!!data.maximumPayOut) {
            result.maximumPayOut = data.maximumPayOut;
        } else {
            result.maximumPayOut = EuroLiteral.create({});
        }
        if (!!data.parseLocation) {
            result.parseLocation = data.parseLocation;
        }
        return result;
    }

    readonly $typename: string = "InsurancePart"; // holds the metatype in the form of a string
    $id: string = ""; // a unique identifier
    parseLocation: FreParseLocation; // if relevant, the location of this element within the source from which it is parsed
    name: string; // implementation of name
    isApproved: boolean; // implementation of isApproved
    statisticalRisk: PercentageLiteral; // implementation of part 'statisticalRisk'
    maximumPayOut: EuroLiteral; // implementation of part 'maximumPayOut'

    constructor(id?: string) {
        super();
        if (!!id) {
            this.$id = id;
        } else {
            this.$id = FreUtils.ID(); // uuid.v4();
        }
        // Both 'observableprim' and 'observableprimlist' change the get and set of the attribute
        // such that the part is observable. In lists no 'null' or 'undefined' values are allowed.
        observableprim(this, "name");
        this.name = "";
        observableprim(this, "isApproved");
        this.isApproved = false;

        // Both 'observablepart' and 'observablepartlist' change the get and set of the attribute
        // such that the parent-part relationship is consistently maintained,
        // and make sure the part is observable. In lists no 'null' or 'undefined' values are allowed.
        observablepart(this, "statisticalRisk");

        observablepart(this, "maximumPayOut");
    }

    /**
     * Returns the metatype of this instance in the form of a string.
     */
    freLanguageConcept(): string {
        return this.$typename;
    }

    /**
     * Returns the unique identifier of this instance.
     */
    freId(): string {
        return this.$id;
    }

    /**
     * Returns true if this instance is a model concept.
     */
    freIsModel(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is a model unit.
     */
    freIsUnit(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is an expression concept.
     */
    freIsExpression(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is a binary expression concept.
     */
    freIsBinaryExpression(): boolean {
        return false;
    }
    /**
     * A convenience method that copies this instance into a new object.
     */
    copy(): InsurancePart {
        const result = new InsurancePart();
        if (!!this.name) {
            result.name = this.name;
        }
        if (!!this.isApproved) {
            result.isApproved = this.isApproved;
        }
        if (!!this.statisticalRisk) {
            result.statisticalRisk = this.statisticalRisk.copy();
        }
        if (!!this.maximumPayOut) {
            result.maximumPayOut = this.maximumPayOut.copy();
        }
        return result;
    }
    /**
     * Matches a partial instance of this class to this object
     * based on the properties defined in the partial.
     * @param toBeMatched
     */
    public match(toBeMatched: Partial<InsurancePart>): boolean {
        let result: boolean = true;
        if (result && toBeMatched.name !== null && toBeMatched.name !== undefined && toBeMatched.name.length > 0) {
            result = result && this.name === toBeMatched.name;
        }
        if (result && toBeMatched.isApproved !== null && toBeMatched.isApproved !== undefined) {
            result = result && this.isApproved === toBeMatched.isApproved;
        }
        if (result && !!toBeMatched.statisticalRisk) {
            result = result && this.statisticalRisk.match(toBeMatched.statisticalRisk);
        }
        if (result && !!toBeMatched.maximumPayOut) {
            result = result && this.maximumPayOut.match(toBeMatched.maximumPayOut);
        }
        return result;
    }
}
