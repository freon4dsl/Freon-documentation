// Generated by the Freon Language Generator.
import { Box, BoxUtil, FreBoxProvider, FreProjectionHandler, BoxFactory } from "@freon4dsl/core";

import { Text2 } from "../../language/gen/index.js";

/**
 * This class implements the box provider for a single node of type Text2.
 * The box provider is able to create the (tree of) boxes for the node, based
 * on the projections that are currently selected by the user.
 * The top of the tree of boxes is always a box of type ElementBox, which is
 * a box that will never be rendered itself, only its content will. Thus, we
 * have a stable entry in the complete box tree for every FreNode node.
 */
export class Text2BoxProvider extends FreBoxProvider {
    constructor(mainHandler: FreProjectionHandler) {
        super(mainHandler);
        this.knownBoxProjections = ["StartEditor", "default"];
        this.knownTableProjections = ["default"];
        this.conceptName = "Text2";
    }

    protected getContent(projectionName: string): Box {
        // console.log("GET CONTENT " + this._node?.freId() + ' ' +  this._node?.freLanguageConcept() + ' ' + projectionName);
        // see if we need to use a custom projection
        if (!this.knownBoxProjections.includes(projectionName) && !this.knownTableProjections.includes(projectionName)) {
            const BOX: Box = this.mainHandler.executeCustomProjection(this._node, projectionName);
            if (!!BOX) {
                // found one, so return it
                return BOX;
            }
        } else {
            // select the box to return based on the projectionName
            if (projectionName === "StartEditor") {
                return this.getStartEditor();
            } else if (projectionName === "default") {
                return this.getDefault();
            }
        }
        // in all other cases, return the default
        return this.getDefault();
    }

    private getStartEditor(): Box {
        return BoxFactory.verticalLayout(this._node as Text2, "Text2-overall", "", [
            BoxUtil.labelBox(this._node as Text2, "This is", "top-1-line-0-item-0"),
            BoxUtil.indentBox(this._node as Text2, 8, "1", BoxUtil.labelBox(this._node as Text2, "literal text", "top-1-line-1-item-0")),
            BoxUtil.indentBox(
                this._node as Text2,
                6,
                "2",
                BoxUtil.labelBox(this._node as Text2, "that is projected in the", "top-1-line-2-item-0"),
            ),
            BoxUtil.emptyLineBox(this._node as Text2, "Text2-empty-line-3"),
            BoxUtil.indentBox(this._node as Text2, 16, "4", BoxUtil.labelBox(this._node as Text2, "editor", "top-1-line-4-item-0")),
            BoxUtil.indentBox(
                this._node as Text2,
                2,
                "5",
                BoxUtil.labelBox(this._node as Text2, "for every concept of type Text.", "top-1-line-5-item-0"),
            ),
        ]);
    }

    private getDefault(): Box {
        return BoxFactory.verticalLayout(this._node as Text2, "Text2-overall", "", [
            BoxFactory.horizontalLayout(
                this._node as Text2,
                "Text2-hlist-line-0",
                "",
                [
                    BoxUtil.labelBox(this._node as Text2, "Text2", "top-1-line-0-item-0"),
                    BoxUtil.labelBox(this._node as Text2, "{", "top-1-line-0-item-1"),
                ],
                { selectable: false },
            ),
            BoxUtil.labelBox(this._node as Text2, "}", "top-1-line-1-item-0"),
        ]);
    }
}
