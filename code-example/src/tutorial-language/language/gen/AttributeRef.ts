// Generated by the Freon Language Generator.
import {
    observablepart,
    FreNode,
    FreUtils,
    FreParseLocation,
    matchElementList,
    matchPrimitiveList,
    matchReferenceList,
    FreNodeReference
} from "@freon4dsl/core";
import { AppliedFeature, AttributeWithEntityType, EntityMetaType } from "./internal";

/**
 * Class AttributeRef is the implementation of the concept with the same name in the language definition file.
 * It uses mobx decorators to enable parts of the language environment, e.g. the editor, to react
 * to changes in the state of its properties.
 */
export class AttributeRef extends AppliedFeature implements FreNode {
    /**
     * A convenience method that creates an instance of this class
     * based on the properties defined in 'data'.
     * @param data
     */
    static create(data: Partial<AttributeRef>): AttributeRef {
        const result = new AttributeRef();
        if (!!data.appliedfeature) {
            result.appliedfeature = data.appliedfeature;
        }
        if (!!data.attribute) {
            result.attribute = data.attribute;
        }
        if (!!data.parse_location) {
            result.parse_location = data.parse_location;
        }
        return result;
    }

    readonly $typename: EntityMetaType = "AttributeRef"; // holds the metatype in the form of a string

    parse_location: FreParseLocation; // if relevant, the location of this element within the source from which it is parsed

    attribute: FreNodeReference<AttributeWithEntityType>; // implementation of reference 'attribute'

    constructor(id?: string) {
        super(id);

        // Both 'observablepart' and 'observablepartlist' change the get and set of the attribute
        // such that the parent-part relationship is consistently maintained,
        // and make sure the part is observable. In lists no 'null' or 'undefined' values are allowed.
        observablepart(this, "attribute");
    }

    /**
     * Returns the metatype of this instance in the form of a string.
     */
    freLanguageConcept(): EntityMetaType {
        return this.$typename;
    }

    /**
     * Returns true if this instance is a model concept.
     */
    freIsModel(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is a model unit.
     */
    freIsUnit(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is an expression concept.
     */
    freIsExpression(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is a binary expression concept.
     */
    freIsBinaryExpression(): boolean {
        return false;
    }
    /**
     * A convenience method that copies this instance into a new object.
     */
    copy(): AttributeRef {
        const result = new AttributeRef();
        if (!!this.appliedfeature) {
            result.appliedfeature = this.appliedfeature.copy();
        }
        if (!!this.attribute) {
            result.attribute = this.attribute.copy();
        }
        return result;
    }
    /**
     * Matches a partial instance of this class to this object
     * based on the properties defined in the partial.
     * @param toBeMatched
     */
    public match(toBeMatched: Partial<AttributeRef>): boolean {
        let result: boolean = super.match(toBeMatched);
        if (result && !!toBeMatched.attribute) {
            result = result && this.attribute.match(toBeMatched.attribute);
        }
        return result;
    }

    /**
     * Convenience method for reference 'attribute'.
     * Instead of returning a 'FreNodeReference<AttributeWithEntityType>' object,
     * it returns the referred 'AttributeWithEntityType' object, if it can be found.
     */
    get $attribute(): AttributeWithEntityType {
        if (!!this.attribute) {
            return this.attribute.referred;
        }
        return null;
    }
}
