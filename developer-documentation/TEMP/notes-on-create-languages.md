In the Courschedule sample:

- CourseSchedule/phase2/src/external/PersonIcon.svelte still uses ExternalSimpleBox => SimpleExternalBox
- CourseSchedule/phase4/src/external/StaffAccordion.svelte still uses ExternalPartListBox => PartListReplacerBox
- CourseSchedule/phase5/src/external/Schedule.svelte still uses ExternalPartListBox => PartListReplacerBox

NB is correct in freon4dsl.

In Insurance sample:

- the external svelte components are missing
